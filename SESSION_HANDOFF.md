# セッション引き継ぎ(2026-08-11 作成 / 2026-08-12 更新)

> **⚠️ このファイルの §1〜§4 は 8/10 時点の情報で、内容が古い。**
> 現行の運用手順は **[CLAUDE.md](CLAUDE.md)** に集約済み(新規セッションで自動読込)。
> このファイルは経緯の記録として残している。
>
> 新規セッションでは:
> 1. **[CLAUDE.md](CLAUDE.md)** — 分析手順・発注ルール・既知の問題
> 2. **[TRADING_CONTEXT.md](TRADING_CONTEXT.md)** — 口座条件・リスク制限・破綻記録
>
> 8/12 の実績と学びは [CLAUDE.md](CLAUDE.md) §9 を参照。

---

## 🔰 新セッションでまずやること

1. **[TRADING_CONTEXT.md](TRADING_CONTEXT.md) を読む** — 口座条件、リスク制限、
   8/7 の失格記録、提示フォーマット(SL/TPは必ず価格で出す)
2. **このファイルの §1 で現状を把握** — ポジションなし、本日 +$242 確定
3. **Telegram Bot を作る**(§4)— ユーザーが BotFather のトークンと
   チャットID を用意済みのはず。無ければ先に依頼する
4. 監視を再開するなら §2 の `/loop` コマンドで cron を再作成

---

## 1. 現在の状態(引き継ぎ時点)

### ポジション: なし(全決済済み)

### 本日(2026-08-10 取引日)の確定損益

| # | 内容 | 損益 |
|---|---|---|
| [1] | 29,865 買い → SL 29,835 執行 | **−$120** |
| [2] | 29,798 売り → TP 29,750 執行 | **+$192** |
| [3] | 29,752 買い → TP 29,796 執行 | **+$176** |
| | 手数料(6ロット往復) | −$6 |
| | **本日確定** | **約 +$242** |

**3戦2勝1敗、枠 $2,000 の12%をプラス。** `order.py --status` で 3/5 回。

### 8/7 との対比(記録として重要)

| | 8/7(失格した日) | **8/10** |
|---|---|---|
| 約定回数 | **66回** | **6回** |
| 最大サイズ | **24枚** | **2枚** |
| 結果 | **−$2,816** | **+$242** |

同じ膠着相場で真逆の結果。**サイズと回数を守れたことが差**。

### 今日効いた判断(次に再現すべきもの)

- **[2] ショート**: 合流帯(C:POC / P:VAH / VWAP の3つが 29,796〜29,806 に重なる)での戻り売り。
  ユーザーが「VWAP中央↑から」と着眼した形
- **[3] ロング**: 「反発確認できたら戻しで入る」と事前に決め、確定足を見てから入った。
  7度機能した支持帯(C:VAL 29,747.96 / P:POC 29,746.94)
- **SL の手動引き上げ**: 29,726 → 29,760 で +$32 を確保。含み損 −$36 まで振れても
  耐えられたのは、リスクが $104 に固定されていたため
- **SL 29,838 への一時拡大**([2]の局面): リスクが $96→$160 に増えたが、
  29,811.5 の戻りを耐えるのに寄与した。**ただし原則1に抵触する判断なので常用しない**

---

## 2. 監視の運用

### cron ジョブ

**3分間隔**で市場分析が自動実行される(ジョブID `0d71dbb5`)。
プロンプト: 「チャート設定を一切変更せずに市場分析。条件成立時のみ Entry/SL/TP を価格で提示。それ以外は「見送り」の1行のみ。」

**新セッションでは cron が引き継がれない**ので、必要なら再作成:
```
/loop 3m チャート設定を一切変更せずに市場分析。条件成立時のみ Entry/SL/TP を価格で提示。それ以外は「見送り」の1行のみ。
```

### チャート構成(絶対に変更しない)

| ペイン | 銘柄 | 時間足 |
|---|---|---|
| 0 | CME_MINI:MNQ1! | 15分 |
| **1** | **CME_MINI:MNQ1!** | **3分** ← 分析対象 |

**毎回 `pane_list` で active_index を確認し、0 なら `pane_focus index=1` で戻すこと。**
ペイン0のデータを誤って使うと15分足の値になり、指標も別物になる。

### 取得手順

```
1. mcp__tradingview__pane_list           (フォーカス確認)
2. mcp__tradingview__pane_focus index=1  (必要なら)
3. mcp__tradingview__data_get_ohlcv count=6
4. mcp__tradingview__data_get_study_values
```

**指標値が前回から大きく飛んだら再取得して確認する。**
ペイン切り替え直後は確定前の値を掴むことがある(実際に2回発生)。

---

## 3. 本日のレベルマップ(3分足)

| レベル | 価格 |
|---|---|
| New York High | 29,900 |
| Prev New York | 29,860.5 |
| **C:POC** | **29,796.83** |
| **Session VWAP** | **約 29,796.6** |
| P:VAH | 29,801.25 |
| C:VAH | 29,842.08 |
| **C:VAL** | **29,747.96** |
| **P:POC** | **29,746.94** |
| VWAP下限 | 約 29,728 |
| P:VAL | 29,682.75 |

**29,742〜29,772 の箱を7回往復した後、上に抜けた。**
29,746〜29,748 の合流帯は本日7度支持として機能。

CVD は本日一貫してマイナス圏(−50,000〜−90,000)。
直近で**ゴールデンクロス成立**(EMA Fast が Slow を上抜け)。

---

## 4. Telegram Bot(2026-08-12 実装完了)

**実装済み。** [telegram_bot.py](telegram_bot.py) / [notify.py](notify.py) /
[snapshot.py](snapshot.py)。使い方は [CLAUDE.md](CLAUDE.md) §6 を参照。

以下は当時の設計メモ(記録用)。

**外出先からエントリー指示を出せるようにする。**

### 構成

```
スマホ(Telegram) → Bot(このPCで常駐) → order.py → CrossTrade → Tradovate
```

### 要件

- `order.py` を呼ぶので**リスク制限がそのまま効く**(1日5回/最大2枚/SL必須)
- 外部公開不要(Telegram Bot API のポーリング)
- ドライラン → 確認 → 送信の2段階

### コマンド案

```
/status              本日の発注回数・建玉状況
/analyze             現在の市場分析(TradingView MCP は使えないので要検討)
/order sell 2 29850 29880 29800    → ドライラン表示
/confirm             直前のドライランを送信
/modify buy 2 29760 29796          → SL/TP 張り替え
/flatten             全決済(要確認)
```

### 必要なもの(ユーザー側)

1. Telegram で **@BotFather** からトークン取得
2. 自分の **チャットID**(@userinfobot で取得可)

### 実装上の注意

- トークンは `.secrets/telegram.env` に置く(会話に出さない)
- **チャットIDでの認証必須**(他人からのコマンドを拒否)
- `--confirm` は Bot 側でも2段階にする
- **Bot 経由でも発注前にユーザー確認を取る**(TRADING_CONTEXT.md の原則)

---

## 5. order.py の現状

### 実装済みコマンド

```bash
python order.py --status                                    # 発注状況
python order.py --side buy --qty 2 --entry X --sl Y --tp Z  # ドライラン
python order.py ... --confirm                               # 送信
python order.py --market --side buy --qty 1 --sl Y --confirm
python order.py --cancel N                                  # 記録から削除(未約定用)
python order.py --flatten --confirm                         # 全決済
python order.py --modify --side buy --qty 2 --sl Y [--tp Z] --confirm  # SL/TP張り替え
```

### `--modify` について(**未検証**)

`cancelandbracket` コマンドを使用。**order_id 不要**で既存ポジションの
ブラケットを張り替えられる。`flatten_first` は意図的に付けていない。

**本番前にデモで検証すること:**
1. ポジションが維持されるか
2. SL が正しく更新されるか
3. **TP 未指定時に TP が消えないか** ← 消えるなら常に両方指定する運用にする

### CrossTrade コマンド一覧(画面から確認済み)

| コマンド | 用途 | order_id |
|---|---|---|
| `place` | 新規発注 | 不要 |
| `cancelandbracket` | **既存ポジションにTP/SL張り直し** | **不要** |
| `change` | 待機注文のqty/limit/stop変更 | **必要** |
| `cancel` | 特定注文の取消 | **必要** |
| `cancelreplace` | 取消して再発注 | 必要 |
| `closeposition` | ポジション決済 | 不要 |
| `flatten` / `flatteneverything` | 全決済 | 不要 |
| `reverse` / `reverseposition` | ドテン | 不要 |
| `flatplace` | 決済して新規発注 | 不要 |

**拡張オプション**: `flatten_first=true`、`require_market_position=flat`、
`max_positions=2`、`start_time`/`end_time`(取引時間制限)、
`take_profit=Xticks`/`Xpts`/`X%`/`$X`(相対指定も可)

---

## 6. 未解決・要注意

- **`--modify` が未検証**(上記参照)
- **ペインのフォーカスが勝手に0に戻る現象**が頻発。毎回確認が必要
- 指標値の不連続が2回発生(ペイン切替直後)。飛んだら再取得
- Telegram Bot では TradingView MCP が使えないため、**分析はPC側のClaude Codeで行い、Bot は発注専用**にするのが現実的
