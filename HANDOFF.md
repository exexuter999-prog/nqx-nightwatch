# 引継ぎ

## 【最新】2026-08-21 — R10: ICT 戦略の取り込み(ADVISORY)

[docs/history/STRATEGY_EVOLUTION_R10.md](docs/history/STRATEGY_EVOLUTION_R10.md) を実装した。
`python tests/run_all.py` → **ALL PASS(11ファイル)**。コード変更は
`msnr_gate.py` のみ(+約210行)。`monitor_publish` / `order` / Bot / Mini App は無変更。

- **既に ICT だった部分**: MSNR の SWEEP 連鎖 = Turtle Soup、FLIP 連鎖 = Breaker。
  **再実装していない**。欠けていたのは「どちら側で・どこへ向かって・いつ」の文脈層
- **新設(全て ADVISORY)**: Premium/Discount+OTE / Draw on Liquidity(LRLR·HRLR) /
  ICT キルゾーン(JST=ET+13h) / 未充填 FVG。`--summary` 末尾と `--card` の
  `advisory.ict` に出る。**promotion には一切影響しない**(実データ400件で差分0)
- **実測**: 過去の `allowed` 143件のうち **96%** が ICT の注意に該当。
  内訳は premium/discount 逆側 73% / HRLR 49% / キルゾーン外 8%。
  → **強制ゲートにはできない**。これは「MSNR が脚の終わりで連鎖を完成させる」
  構造の測定値で、使い道は拒否ではなく **Entry の置き直し**(R10 §4.2)
- **2026-08-21 の −$115 は ICT で説明できる**: SL 29,355.75 は
  ディーリングレンジの **EQ 29,355.25 の 0.5pt 上**、最大逆行 29,356.50 は
  EQ をわずかに超えた点。Entry は DISCOUNT 37%(売ってはいけない側)で、
  ICT の OTE 売り帯は 56-95pt 上にあった
- **副産物**: 追加テストが `dealing_range` の **OTE 売買が逆だったバグを検出**し修正
- **次の判断**: 3セッション観測後に (a) ランチ帯(01:00-02:00 JST)の武装停止
  (b) HRLR の A+ 限定 — の2つが ENFORCED 昇格の最有力候補
- **Index SMT も実装した(R10 §5)**: NQ/ES/YM の高安を比べ、1つだけが更新しない
  乖離を検出。**価格だけで計算する**ので 8/19 の CVD キャッシュ事故では止まらない。
  `snapshot.peers` に ES/YM の足を入れると動く(無ければ `SMT: 未取得` = 中立)
- **SMT 用タブ = index 1(chart_id `2RiggmK6`)に確定**(2026-08-21 ユーザー承認)。
  実走で ES を取得 → **タブ1を MNQ1!/3分/指標5つに復帰済み**・タブ0も無傷を確認。
  結果は `SMT ES: 乖離なし`(NQ/ES とも安値HL・高値LH)
- **★ SMT 実走で判明した罠3点**(手順は CLAUDE.md §3 に反映済み):
  (a) `data_get_ohlcv` / `quote_get` の `symbol` 引数は**無視される**
  (b) `chart_set_symbol` 直後に**フォーカスがタブ0へ飛ぶ** → `tab_switch` をやり直す
  (c) **YM1! はデータが出ない**(赤✗・取得失敗)= CBOT 配信権限が無いと思われる。
  **SMT は ES 単独で成立する**ので当面 ES のみで運用。`tab_new` は success を
  返すが**タブは増えない**
- **未実装**: CBDR·Asian Range STD / ADR 投影 / Power of 3(R10 §6)


## 【最新】2026-08-21 01:2x — Apex 初トレード = SL(−$115)。原因は私の設定変更

**結果**: SHORT 2枚 29,327 → SL 29,355.75。**−$115.00**(28.75pt × 2枚)。
Apex 口座の初戦。

| 反映先 | 状態 |
|---|---|
| リザルトカード | `rs_48a2d3dd58e2e260a22d` を publish 済み(アプリに表示される) |
| LEDGER(記録タブ) | `recentResults` **2件**に載っているのを実測確認 |
| LIFELINE(アプリ残高) | **$3,000 → $2,880**(ユーザー指定値。価格計算上は $2,885 なので **$5 の差**は手数料/実約定差として要突合) |
| dayguard | **日次−$480・2連敗・損切り後15分冷却**。台帳不能は新規のみ `DAYGUARD_UNAVAILABLE` |

### チャート実測による敗因分析(1分足の実約定 + 3分足)

**約定**: SELL 2 @ 29,327.00 → SL 実約定 **29,356.25**(記録は 29,355.75。
0.5pt の滑り。`resultId` はハッシュなので**訂正 publish は二重掲載**になるため未修正)。

3分足の実測:

| 時刻 | 足 | 出来事 |
|---|---|---|
| 01:21 | o 29,319.5 → h **29,354.5** → c 29,342.75 | **上昇35ptの足の中で指値が約定**。戻りの拒否は一度も起きていない |
| 01:24 | h **29,356.50** | **最大逆行。ここで SL 執行** |
| 01:27 | c 29,340.75 | 反転開始 |
| 01:30 | l **29,309.75** | 方向は正しかった(Entry −17.25pt) |

**敗因は4つ、うち2つは機械化できた:**

1. **★ ストップが最大逆行に 0.75pt(3tick)足りず、予算は 36% 余っていた。**
   SL 28.75pt = **1.06 × ノイズ床**(最小緩衝)。SL上限 45pt に対し使用率 **0.64**。
   **1.5×NF = 40.68pt(リスク $163)なら予算内で生存**していた。
   → `msnr_gate.stop_budget()` として実装・**拒否に使う**(CLAUDE.md §2)
2. **★ VP の回転目標がエントリー時点で使い果たされていた。**
   `VP_ACCEPTED → target 29,325.35` に対し Entry 29,327 = **残り 1.65pt(0.06×NF)**。
   回転の終点で売り、TP はその 60pt 先。→ `msnr_gate.vp_headroom()` として実装
   (REPORT-ONLY)
3. **エントリーがモメンタム足に轢かれた。** 空気幅 7% / 到達 0.54R は通っており、
   **既存ゲートでは検出できない**。指値は「触れれば約定する」ので、
   拒否の確認を待つ型ではない。**未解決**(§R8 の管理計画とは別問題)
4. **CT_ATR 61.56pt に対し SL 28.75pt = 0.47倍。** 15分足1本の半分以下。
   形式的には「3分反応だから3分ノイズ床でよい」(operational-gates §7)を
   満たすが、**CT_ATR ÷ ノイズ床 = 2.27** という HTF が荒れた局面では保たない。
   → 1. の 1.5×NF 規則が実質的にここを埋める

**注意: ストップを広げても勝ちではなかった。** 最大順行は 17.25pt(0.6R)で
**TP 29,265 には届いていない**。1. は「この負けを消す」規則であって
「この勝ちを作る」規則ではない。

### 実装(R9・テスト込み)

| 追加 | 内容 |
|---|---|
| `msnr_gate.stop_budget()` | SL距離 / 上限 / 使用率 / NF倍率 / 推奨(1.5×NF)/ pass |
| `msnr_gate.vp_headroom()` | VP目標までの残り pt・NF倍・exhausted |
| `--card --entry E --stop S` | 上2つを `stopBudget` / `vpHeadroom` として出力 |
| 後方互換 | **entry/stop を渡さない既存呼び出しは1バイトも変わらない**(テストで固定) |
| `tests/test_msnr_gate.py` | R9 セクション5本 |
| `tests/test_events.py` | **密閉修正**: 実台帳を読んでいたため「その日に損切りがあると落ちる」状態だった(今日実際に落ちた)。dayguard をスタブ化 |

`python tests/run_all.py` → **ALL PASS(11ファイル)**。
今回の実データでの出力:
`stopBudget {util 0.64 / nfMult 1.06 / wantPt 40.68 / pass False}` /
`vpHeadroom {remainPt 1.65 / exhausted True}` — **どちらも武装前に不合格を出せた。**

### 経緯(短く)

1. Mini App のカードが **5行すべて✔なのに `NOT ARMED`**。ユーザーが不具合を疑う
2. 実測すると止めていたのは **grade=A**(「A+のみ」帯の等級要件)。
   **grade はチェックリストの5行に無い**ので、画面上は理由が見えない = **UI 欠陥**
3. さらに**私が監視ループ稼働中に `CARD_SL_CAP` を 52.5 → 90 → 45 と2度変えた**結果、
   1枚のカードに**3つの比率が混在**していた:

   | 表示 | 比率 | 由来 |
   |---|---|---|
   | ボラ予算行 `0.33 通常帯 ✔` | 29.25 ÷ 90pt | 1枚設定時に生成 |
   | 理由文 `比率0.557` | 29.25 ÷ 52.5pt | Lucid 時代の値が焼き付き |
   | 現在 `0.60 A+のみ` | 27.12 ÷ 45pt | 2枚設定(正) |

   **通常帯なら grade は問わない**ので、01:02〜01:10 のカードは
   **本来 ARMED になるべきだった**(監視側が古い 0.557 を理由文に固定していた)
4. 構造ゲートは全通過だったため、**grade=A のまま手動で発注**(ユーザー承認)。
   板・OCO をブローカー側で確認 → 約定 → **SL 執行**

### 学び(次に効くもの)

- **監視ループ稼働中に判定パラメータを変えない。** 変えるならループを止める。
  今回は1枚→2枚の途中経過(90pt)がそのままカードに焼き付いた
- **理由文に比率などの計算値を焼き込まない。** シナリオが持ち越されると腐る
- **ブロック理由がチェックリストに出ない**のは直す価値がある(grade 行の追加)
- 手動発注は「ゲートを通した武装」ではない。**記録上もそう扱う**

### 未裁定のまま残っている論点(今回の直接原因)

`grade_of` 条件3 は鮮度 ∈ {FRESH, WICK_TESTED, FLIPPED} を要求するが、
`FLIPPED` は **FLIP 連鎖が完成したときにしか付かない**。今回のように
**破壊されたレベルの反対側を SWEEP 経路で取る**と鮮度は `BROKEN` のままなので、
条件1(初動 47.75 ≥ 35.26)・条件2(合流あり)を満たしても **A+ になれない**。
R2 は `anchorOk[SELL]=true` と認めているので、**R5 裁定①と同型の穴**。
**この修正を「今のトレードを通すため」に入れるのは取り逃しへの反応なので未実装。**

## 【最新】2026-08-21 — Apex 100K へ全面移行(接続実証済み)

### CrossTrade を再照会した結果、旧3口座のうち2つが消えていた

`GET /v1/api/tv/accounts` の実測(この照会で判明):

| 口座 | CrossTrade 側 | 対応 |
|---|---|---|
| **APEX4568750000009** | 生存(accountId 62816562 / demo / 2026-08-20 リンク) | **唯一の発注先に設定** |
| LFE02562316710003 | 生存・FLAT(61965347) | ユーザー指示で不使用 |
| LFE02562316710002 | **`unknown_account`** | 経路から削除 |
| LFE02562316710004 | **`unknown_account`** | 経路から削除 |

**`.secrets/crosstrade.env` は …0002/…0004 を含んだままだった** —
気付かずに発注していれば3件中2件が失敗していた。

### 接続テスト(実弾・約定しない指値方式)

```
python order.py --side buy --qty 1 --entry 29000 --sl 28960 --tp 29080 --last 29286.5 --confirm
→ HTTP 200 {"success": true}
```

REST `GET /accounts/APEX4568750000009/orders` で**実際に板に乗ったことを確認**:

| id | action | ordStatus | 意味 |
|---|---|---|---|
| 629991140004 | Buy | **Working** | 親の指値(現在値 29,286.5 の 286pt 下・約定しない) |
| 629991140005 | Sell | Suspended(ocoId 対) | **SL** — 親の約定待ちで待機 |
| 629991140006 | Sell | Suspended(ocoId 対) | **TP** — 同上 |

→ **webhook 認証 → 口座解決 → 指値発注 → OCO ブラケット自動生成 → REST 照会**
まで一気通貫で成立。netPos は 0 のまま(意図どおり未約定)。

後始末: `--flatten --confirm`(= `flatteneverything` なので**作業中注文も取消**)
→ 板 **0件**を再照会で確認 → `--cancel 1` で発注回数も戻した(0/3)。
**残骸ゼロ。**

### 再定義したリスク管理

| 項目 | 旧(Lucid 3口座) | **新(Apex 100K intraday)** |
|---|---|---|
| 発注先 | …0002/…0003/…0004 | **APEX4568750000009 単独** |
| 1トレード | $105 ×3 | **$180**(= $3,000 × 0.06) |
| 枚数 | 最大2枚 | **常に 2枚(固定)** |
| SL 上限 | 52.5pt | **45pt**($180 ÷ $2.00 ÷ 2枚) |
| 日次損失 | −$210 | **−$480**($240×2連敗) |
| DAYGOAL | 625/370/212 | **$1,800**(目標$6,000 の30% ※未確認) |
| LIFELINE(アプリ表示額) | 計 $1,359.5 | **$3,000** |

反映先: `.secrets/crosstrade.env`(旧口座の設定は
`#(2026-08-21 封印)` で**消さずに保存**・バックアップ `crosstrade.env.bak-20260821`)/
`CLAUDE.md` §冒頭・§2・§3・§7 / `TRADING_CONTEXT.md` §1・§3・§5・§6.4 /
`msnr_gate.CARD_SL_CAP` **52.5 → 45.0**(表示と強制のズレを再発させないため。
テスト §24 の回帰値も追随)。`python tests/run_all.py` → **ALL PASS(11ファイル)**。

Mini App への publish 実測: `{"id": "APEX4568750000009", "cap": 180.0, "buffer": 3000.0}`。

### ★ 引き継ぐ問題(3件)

1. **ボラ予算ゲートは「一部回復」した(2枚化の効果)。** 比率 = ノイズ床 ÷ **45pt**。
   1枚(90pt)では停止帯にノイズ床 54pt 超が必要で事実上死んでいたが、
   45pt では 8/17 の2敗の武装時点(19.25 / 20.50pt)が **0.43 / 0.46 = A+のみ帯**、
   22-23時の中央値 25〜30pt が **0.56 / 0.67 = A+のみ〜停止**に戻った。
   ただし $60(25pt)時代の「両方とも全停止」までは戻っていない。
   **抑止の主役は MSNR 連鎖ゲート(8/17 の2敗は `FLIP_RETEST_NOT_HELD` で停止)、
   ボラゲートは従**という役割分担で運用する
2. **`order.py` の `MAX_ORDERS_PER_DAY = 3` が生きている。**
   CLAUDE.md §3 は「発注回数の上限は無い」(2026-08-18 撤廃)と書いているのに
   コードは3回で止める。**4回目が拒否される。**必要利益 33R の口座では
   実際に当たる。上げるか撤廃するかは**リスクを増やす変更なのでユーザー裁定**
3. **DAYGOAL $1,800 の根拠(Apex consistency 30%)は一次確認していない。**
   規約または管理画面で実値を確認し、違えば `DAYGOAL_APEX4568750000009` を直す

### R8(High RR)への影響

**2枚固定なので分割型・単枚ランナー型の両方が使える。**
分割型(1枚 TP1 / 1枚ランナー)は **SL ≤ 45pt** が条件。
`--modify` のライブ検証(R8 Phase B 3.1)は依然として最優先 —
**建値移動が動かなければどちらの型も成立しない。**

### 実測した安全装置(2枚での再確認)

```
2枚 SL 45pt = $180 ちょうど  → 通る
2枚 SL 46pt = $184           → 除外 → ERROR「SL を近づけるのではなく見送ってください」
3枚                          → ERROR「最大 2 枚です」(order.py MAX_QTY=2)
publish 実測                 → {"id":"APEX4568750000009","cap":180.0,"buffer":3000.0}
```
