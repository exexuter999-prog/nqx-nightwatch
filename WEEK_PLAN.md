# LTA 2口座 合格プラン

最終更新: 2026-08-24（旧 Lucid 3口座版から全面差し替え）

口座・金額の正本は `TRADING_CONTEXT.md` §1。この文書は**通し方の段取り**だけを書く。

---

## 0. 先に言っておくこと

**「確実に合格」は作れない。** 勝率も期待値も、現時点で**測定されていない**。
実トレード記録は1件しかなく、403本のサイクル記録に約定結果が1件も入っていない。

作れるのは次の2つだけで、この計画はそれに徹する。

1. **1トレードでも1日でも口座が死なない構造**にする（算術で保証できる）
2. **エッジが有るのか無いのかを、死なないうちに測る**（期限が無いので時間は無料）

期限が「最低5日・上限なし」である以上、**急ぐ理由が一つも無い**。
早く通そうとしてサイズを上げる行為は、期待値ではなく分散を買うだけになる。

---

## 1. 口座条件

| | LTA-A | LTA-B |
|---|---|---|
| 口座名 | `LTATANOBA1000000000001` | `LTATANOBA1000000000002` |
| accountId | 90000001 | 90000002 |
| 利益目標 | $6,000 | $6,000 |
| 最大損失 | $3,000（**EOD トレーリング**） | $3,000（同） |
| 一貫性ルール | 無し | 無し |
| 期間 | 最低5日・上限なし | 同 |
| 現在の状態 | **稼働（ミラー発注先）** | **稼働（ミラー発注先）** |

比率は **2.0 : 1**。残機の2倍を稼ぐ。

R40で通常2枚固定の2口座ミラー経路を開通した。ENTRYは1 claim・1 route、
管理と決済は `--account` でA/B別に行う。どちらか一方の照会が不明なら新規を止め、
一方の建玉を他方の状態から推測しない。

---

## 2. 毎日の段取り

### 寄り付き前（1回）

```powershell
cd "C:\Users\exexu\Downloads\nq-nightwatch-claude-code-handoff"
python broker_status.py --accounts      # ★ 口座が消えていないか
python broker_status.py --account LTATANOBA1000000000001 --json
python broker_status.py --account LTATANOBA1000000000002 --json
python order.py --status
python events.py --refresh
```

**`--accounts` に発注先が出てこなければ、その日は発注しない。**
2026-08-21 と 2026-08-24 の二度、設定に残った口座が CrossTrade 側から消えていた。
口座は黙って消える。

### ★ LIFELINE の更新（毎日・省略不可）

EOD ドローダウンは終値で上へ切り上がる。`LIFELINE_*` は**ブローカーから取得できず、
コードにトレーリングの実装も無い**ので、毎日その日の終値から手で書き換える。

```
LIFELINE_LTATANOBA1000000000001=<新しい残機>
```

更新漏れ = 残機の誤認。可変枚数（Phase 3）を入れた後は、そのままサイズ誤りになる。

### 引け後

- その日の損益を `.secrets/day_ledger.jsonl` の記録と突き合わせる
- 新しい残機を計算して `LIFELINE_*` を書き換える
- DAYGOAL に届いた日は、翌日の床が上がっていることを意識する

---

## 3. リスクの置き方

| 項目 | Phase 2（現在） | Phase 3（予定） |
|---|---|---|
| 1R | **$240**（契約上限） | $300（残機の 1/10） |
| 枚数 | **2枚固定** | リスク額から逆算（可変） |
| SL 上限 | **60pt** | リスク額 ÷ 枚数 |
| 日次損失上限 | **−$480**（2連敗） | −$600 |
| DAYGOAL | **$900** | 同 |
| 残機での耐久 | **12回** | 10回 |

**残機10回分は一般的な目安より薄い。** 破産確率の観点では 20〜50 単位が定石で、
10 単位は 10 連敗で死ぬ。期限が無い以上、単位を薄くする理由は時間短縮しか無い。
それでも 10 で行くなら、日次ガードを確実に効かせること。

### 枚数は交渉の対象ではない（Phase 2 の間）

「2枚固定」はリスク調整の手段を **SL 幅だけ**に限定するという設計判断。
構造が 60pt を超える SL を要求するなら、**枚数を減らして通すのではなく見送る**。
ここを緩めた瞬間に「枚数で調整する」思考が戻る。

### 金額を理由に SL を詰めない

`TRADING_CONTEXT.md` §6 の破綻パターンそのもの。2026-08-15 に、1枚 $60 枠に対して
**SL 13pt**（その日の3分足の平均値幅 15pt）で提示した実例がある。
**「普通の1本」で刈られる幅**を、金額が小さいという理由で採用していた。

残機が $4,000 → $3,000 に減ったぶん、この誘惑は前より強く働く。
順序は **構造 → 緩衝 → 距離 → 枚数**。金額から逆算しない。

---

## 4. 止まる条件

| 条件 | 動作 |
|---|---|
| 日次 −$480 | その日は終了 |
| 2連敗 | その日は終了 |
| 損切り後 15分 | 新規なし（冷却） |
| DAYGOAL $900 到達 | その日は終了 |
| `--accounts` に発注先が無い | その日は発注しない |
| `broker_status --json` が `verified≠true` | `HALT` |
| 台帳が壊れている / 未知の状態 | `HALT` |

**刈られた直後の入り直しをしない。** 本人が自覚している破綻要因で、
2026-08-07 の 6分5往復がその実例。冷却15分はそのために置いてある。

---

## 5. 最初の5日は較正期間として使う

最低取引日数が5日ある。これを**エッジの測定**に充てる。

- サイズは Phase 2 のまま（1R $240 = 残機の 8%）
- 毎トレードの結果を記録する。**特に runner が最終目標へ届いた頻度**
  （現在の R 見積もりは「目標の R」であって実現値ではない。ここが最大の未知）
- 5日終わった時点で、勝率・実現 R・TP1 到達率を出す

そのうえで:

| 測定結果 | 次の手 |
|---|---|
| 期待値がプラス | Phase 3 でサイズを上げる。時間は無制限なので焦らない |
| 期待値がゼロ付近 | サイズを上げない。エッジの改善が先 |
| 期待値がマイナス | **通す前に戦略を直す。**サイズで取り返そうとしない |

**このステップを飛ばして 1R を $300 に上げるのは、未知の期待値に賭け金を積む行為。**

---

## 6. Phase の進み方

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | 口座一覧の取得（`broker_status.py --accounts`） | **完了** |
| 2 | 新条件の設定と文書。2枚固定で運用 | **完了** |
| 3 | リスク額からの可変枚数（単一口座で完成させる） | 未着手 |
| 4 | 2口座ミラー（1 claim・口座別照会/管理/決済） | **R40 完了** |

Phase 4 はclaimを口座別に増やさず、1シグナル1 claimのまま4脚を凍結した。

詳細は `docs/R38_LTA_ACCOUNT_MIGRATION.md`。
