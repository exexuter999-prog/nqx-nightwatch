# トレード運用コンテキスト(Claude Code / Codex 共通)

最終更新: 2026-09-08 (Lucid Flex 50K 評価通過 → funded 口座 LFF00000000000006 へ入替・一貫性ルールなし / 基盤は R40)

このファイルは、MNQ 先物のプロップファーム・チャレンジ運用に関する
**確定済みの前提条件**をまとめたもの。分析・発注の前に必ず読むこと。

> **口座 ID の表記(2026-09-15)**: リポジトリを外部(Devin)と共有するため、この文書とテスト内の
> 口座 ID は **0 埋めの伏せ字**に置換した(例: `LFF00000000000006` = Lucid Flex 50K funded、
> `LFE00000000000024` = Lucid Flex 50K 評価、`LTATANOBA1000000000001/2` = LTA-A/B、
> `LTF00000000000005` = LucidPro 50K、`LFF00000000000002` = Lucid Flex 25K、
> `APEX0000000000009/10` = Apex。Tradovate の数値 accountId は `9000xxxx`)。
> **実 ID の正本は `.secrets/crosstrade.env` の `CROSSTRADE_ACCOUNTS` と
> `CROSSTRADE_ACCOUNT_ID_<口座ID>`**。文書の ID を発注先として使わない。

---

## 1. 口座条件(2026-08-24 再定義・CrossTrade 実測)

**現在の発注先は Lucid Flex 50K funded `LFF00000000000006` の1口座(2026-09-08〜)。**
条件と経緯は次節「★ 2026-09-08」。評価口座 LFE00000000000024(09-04〜09-08)と
LTA 2口座(08-24〜09-04)の記述はそれ以降の節に履歴として残す。

(旧)**LTA の A/B の2口座ミラー。**同一シグナルを各口座2 MNQで送る。
（2026-08-27 に LFF を追加 → 08-31 に LucidPro 50K へ入替 → **09-01 に残機超過で封印**。
経緯は次節。`execution_contract.json` の `accountMode.maxAccounts` は
**`null`＝上限なし**のままなので、口座を戻すときは env と `cloudflare/wrangler.toml` を
揃えて deploy すればよい。）

| 項目 | 値 |
|---|---|
| 発注先(A) | **`LTATANOBA1000000000001`**(accountId 90000001 / `demo`) |
| 発注先(B) | **`LTATANOBA1000000000002`**(accountId 90000002 / `demo`) |
| 利益目標 | **$6,000**(各口座) |
| 最大損失(= 残機 / LIFELINE) | **$3,000**(各口座) |
| DD 方式 | **EOD トレーリング**。日中は動かず、終値で上にだけ切り上がる |
| 一貫性ルール | **無し** |
| 期間 | **最低5日・上限なし** |
| **1トレードのリスク** | **$240**(= 契約上限。Phase 3 で $300 へ) |
| **枚数** | **常に 2枚(固定)**(Phase 3 で可変枚数へ) |
| **SL 上限** | **60pt**($240 ÷ $2.00 ÷ 2枚) |
| 日次損失上限 | **各口座−$480 / 2口座合算−$960**(= 1シグナル合算$480 × 2連敗) |
| DAYGOAL | **$900**(利確ブレーキ。下記のとおり意味が変わった) |

比率は **$6,000 ÷ $3,000 = 2.0 : 1**(旧 Apex の 2.25:1 よりわずかに有利)。

### ★ 2026-09-08: Lucid Flex 50K 評価通過 → funded 口座 LFF00000000000006 へ入替

CrossTrade 実測(17:54 JST)。評価口座 `LFE00000000000024` が `/accounts` から消え、
funded 口座 `LFF00000000000006` が出現した(口座は黙って消える: 3 度目)。同日の実績は
BREAKER_CONTINUATION SELL B 14 枚(ULTRA)+$938(手数料 $14)、7 日累計 ≈ +$3,164 で
評価目標 $3,000 を超えた直後だった。ユーザー確認: **評価通過・funded・一貫性ルールなし**。

| 項目 | 値 |
|---|---|
| 発注先 | **`LFF00000000000006`**(accountId 90000006 / `demo`) |
| 口座残高 | $50,000(初期。実残高は `build_accounts_payload` の equity で実測) |
| 利益目標 | **無し**(funded。出金ルールは別) |
| 最大損失(MLL = 残機) | **$2,000** の EOD トレーリング(`TRAILING_DD_LFF00000000000006=2000`)。**残高が $52,100 を超えて日を終えると最低残高は $50,100 で固定**(2026-09-14 ユーザー確認。`DD_LOCK_FLOOR_LFF00000000000006=50100`)。9/11 の EOD $53,657 で固定済み → 床 $50,100 |
| DD 方式 | EOD トレーリング → 開始 + $100 で停止(床 = min(EOD 高値 − 2,000, 50,100)。Lucid 50K funded の規則) |
| 一貫性ルール | **無し**(2026-09-08 ユーザー確認) |
| **1トレードのリスク(通常経路)** | **$200**(= 残機の 1/10) → SL 上限 **50pt**(2枚固定) |
| DAYGOAL | **$800**(表示・集計専用) |
| ULTRA | 対象口座・利益目標は **Mini App の口座別設定で入れ直す**(旧 LFE の設定は新口座に引き継がれない) |

`.secrets/crosstrade.env` で動かしたのは 4 点(バックアップ `crosstrade.env.bak-20260908-lff50k`):
`CROSSTRADE_ACCOUNTS` を LFF…0006 のみへ、`CROSSTRADE_ACCOUNT_ID_LFF00000000000006=90000006` を追加、
LFE…0024 の `RISK_ / LIFELINE_ / TARGET_ / DAYGOAL_ / TRAILING_DD_ / ACCOUNT_ID` を封印、
LFF…0006 の `RISK_=200 / LIFELINE_=2000 / DAYGOAL_=800 / TRAILING_DD_=2000` を追加(`TARGET_` は無し)。
`NQX_DAY_LOSS_LIMIT=-400` は据え置き。`cloudflare/wrangler.toml` の `NQX_AUTOTRADE_ACCOUNTS` も
同じ集合へ直し、**deploy しないと AUTO は武装できない**(Worker の arm が旧 scope を発行する)。

同日に見つけた不備(R68): 1 口座運用で AUTO が OFF/失効すると所有済み建玉の管理が止まっていた。
修正済み(`autotrade_engine._disarmed_single_account_management`)。

### ★ 2026-09-04: LTA 2口座が EOD 床割れ → Lucid Flex 50K 評価(1口座)へ入替

CrossTrade 実測(23:00 JST)。LTA 2口座は `/accounts` にまだ `OK` で出るが、
R44 の床(高値台帳 − TRAILING_DD 3000)を割っている。

| 口座 | Net Liq | 床 | 判定 |
|---|---|---|---|
| LTATANOBA1000000000001 | $97,629.50 | $97,663.70 | **床を $34.20 割った** |
| LTATANOBA1000000000002 | $97,663.80 | $97,704.60 | **床を $40.80 割った** |
| **LFE00000000000024** | **$50,000.00** | $48,000 | 無傷(残機 $2,000) |

ユーザー指示で **Lucid Flex 50K 評価 1口座**へ発注経路をつなぎ替え、当日は
**ULTRA**(Mini App の口座別設定 → publish 時サイジング、R47)で運用する。

| 項目 | 値 |
|---|---|
| 発注先 | **`LFE00000000000024`**(accountId 90000024 / `demo`) |
| 口座残高 | **$50,000**(`build_accounts_payload` の equity で実測) |
| 利益目標 | **$3,000** |
| 最大損失(MLL = 残機) | **$2,000**(`TRAILING_DD_LFE00000000000024=2000`) |
| DD 方式 | **EOD トレーリング**。日中は動かず終値で切り上がる。日次損失制限は無し |
| 一貫性ルール | **評価中は 50%**(最大日次利益 ≤ 総利益の 50%) |
| **1トレードのリスク(通常経路)** | **$200**(= 残機の 1/10) → SL 上限 **50pt**(2枚固定) |
| DAYGOAL | **$800**(表示・集計専用) |
| ULTRA | 対象口座 = この1口座 / 利益目標 $3,000 / 上限 = min(残DD, 契約 $5,000) |

**★ ULTRA と一貫性ルールは食い合う。** ULTRA は「1トレードで目標に届く枚数」を
出すので、1日で $3,000 取ると最大日次利益が総利益の 100% になり、評価は
その日には通らない(総利益 $6,000 まで積むか、Mini App の利益目標を下げる)。
これは設計上の性質であって不具合ではない。

`.secrets/crosstrade.env` で動かしたのは 4 点 + 新口座ブロック(バックアップ
`crosstrade.env.bak-20260904-lfe50k`):

| キー | 変更 |
|---|---|
| `CROSSTRADE_ACCOUNTS` | `LFE00000000000024` のみ |
| `CROSSTRADE_ACCOUNT_ID_LFE00000000000024` | `90000024` を追加 |
| `RISK_ / LIFELINE_ / TARGET_ / DAYGOAL_ / TRAILING_DD_ LTATANOBA…` | 封印(`#(2026-09-04 封印/残機超過)`) |
| `NQX_DAY_LOSS_LIMIT` | −960 → **−400**($200 × 1口座 × 2連敗) |
| 新規 | `RISK_=200 / LIFELINE_=2000 / TARGET_=3000 / DAYGOAL_=800 / TRAILING_DD_=2000` |

`cloudflare/wrangler.toml` の `NQX_AUTOTRADE_ACCOUNTS` も `LFE00000000000024` へ直した。
**deploy(`cd cloudflare && npm run deploy`)しないと AUTO は武装できない**(Worker の
arm が旧 scope を発行し、ローカルと食い違って即 DISARMED になる)。

### ★ 2026-09-01: LucidPro 50K が残機超過 → 封印。LTA 2口座へ戻した

| 口座 | Net Liq | 実現 | 床 | 判定 |
|---|---|---|---|---|
| LTATANOBA1000000000001 | $100,663.70 | **+$452.40** | — | 無傷。DAYGOAL $900 未達 |
| LTATANOBA1000000000002 | $100,704.60 | **+$491.60** | — | 無傷。DAYGOAL $900 未達 |
| **LTF00000000000005** | **$47,950.50** | **−$2,049.50** | **$48,000** | **床を $49.50 割った** |

床は `$50,000 − TRAILING_DD 2000 = $48,000`。**1日で 1R $200 の 10.2 回分**を使い切った。
§3 の「残機10回分は破産確率の観点で薄い(定石は 20〜50 単位)」がそのまま出た形で、
相場ではなく**単位の薄さ + 回転**が原因。LTA 2口座は同じ日に合算 +$944.00 で無傷。

CrossTrade の `/accounts` は 05:07 時点で3口座とも `OK` を返していたが、**口座は黙って
消える**(2026-08-21 / 08-24 に2度)。宛先不明のまま送りつづけないよう、プロップ側の
判定を待たずに経路から外した。

`.secrets/crosstrade.env` で同時に動かしたのは4点:

| キー | 変更 |
|---|---|
| `CROSSTRADE_ACCOUNTS` | LTA 2口座のみへ |
| `CROSSTRADE_ACCOUNT_ID_LTF00000000000005` | 封印 |
| `RISK_ / LIFELINE_ / DAYGOAL_ / TRAILING_DD_LTF00000000000005` | 封印(`dayguard.configured_accounts()` が `RISK_*`/`DAYGOAL_*` のキー名から口座を逆引きするため、片方だけ残すと集計倍率が狂う) |
| `NQX_DAY_LOSS_LIMIT` | −1360 → **−960**($240 × 2口座 × 2連敗) |

**さらに `cloudflare/wrangler.toml` の `NQX_AUTOTRADE_ACCOUNTS` も同じ集合へ直して
deploy した**(Worker Version `8ef3066d`)。ここは env とは別管理で、最初の修正では
取りこぼしていた。**ローカルが2口座・Worker が3口座のままだと `autotrade_arm` が毎回
「account scope changed since arming」で武装を失効させ、Mini App で AUTO を何度 ON に
しても新規 ENTRY が永久に出ない**(wrangler.toml 自身の警告コメント / 2026-08-27 実測)。

**AUTO は設計どおり自動 DISARMED になった。**実測:
`DISARMED (CROSSTRADE account scope changed since arming; re-arm in app)` /
`accounts: 2 configured`。Mini App で張り直すまで新規 ENTRY は出ない。

### ★ 2026-08-31: Lucid Flex 25k 評価 → LucidPro 50K(DD $2,000)へ入替完了

評価口座 `LFF00000000000002` は合格し、**LucidPro 50K(最大損失 $2,000)**へ移行した。

経過(いずれも CrossTrade 実測):

| 時刻 | `/accounts` | 残高照会 | 経路 |
|---|---|---|---|
| 22:06 | 旧 LFF が残る / 新口座は**出てこない** | 旧 LFF が **HTTP 400** = 実質死 | 旧 LFF を外し **LTA 2口座**へ |
| 22:16 | 旧 LFF が**消えた** / **`LTF00000000000005`** が出現 | 新口座 `verified=true` / equity $50,000 | **LTA A/B + LucidPro の3口座**へ |

**当時の発注先(3口座)** — ★ 2026-09-01 に (C) は残機超過で封印。現在は LTA 2口座。

| 項目 | 値 |
|---|---|
| 発注先(C) | **`LTF00000000000005`**(accountId 90000005 / `demo`)**【2026-09-01 封印】** |
| 口座残高 | **$50,000**(`build_accounts_payload` の equity で実測) |
| 最大損失(= 残機) | **$2,000**(`TRAILING_DD_LTF00000000000005=2000`) |
| DD 方式 | **EOD トレーリング**(旧 Lucid と同じ。残機は R44 の自動導出) |
| **1トレードのリスク** | **$200**(= 残機の 1/10。旧 LFF の $1,000→$100 と同比率) |
| **SL 上限** | **50pt**($200 ÷ $2.00 ÷ 2枚) |
| DAYGOAL | **$800**(日次損失上限 $400 の2倍。LTA/旧LFF と同比率) |
| 利益目標 | **未確認**。`TARGET_LTF00000000000005` は未設定 |

`NQX_DAY_LOSS_LIMIT` は当時 **`-1360`**(= LTA $240 + LTA $240 + LucidPro $200 = $680 × 2連敗)。
**2026-09-01 に LucidPro を外して `-960` へ戻した。**

**★ SL 50pt が境界。** ドライラン実測:

```
SL 50pt → リスク $200.00  発注先 LTA-A / LTA-B / LucidPro(3口座)
SL 60pt → リスク $240.00  除外 LTF00000000000005: リスク $240.00 が上限 $200.00 を超過
```

`accounts.sync` は `missing/dead/unknown` すべて空になった(旧 LFF の unknown も解消)。
AUTO は口座スコープ変更で**自動 DISARMED**のまま。Mini App で張り直すまで新規 ENTRY は出ない。

### Lucid Flex 25k(2026-08-27 合格・3口座目として追加 / 2026-08-31 に封印)

| 項目 | 値 |
|---|---|
| 発注先(C) | **`LFF00000000000002`**(accountId 90000102 / `demo`) |
| 口座残高 | $25,000 |
| 利益目標 | **$2,500** |
| 最大損失(= 残機 / LIFELINE) | **$1,000** |
| DD 方式 | **EOD トレーリング**（LTA と同じ。`TRAILING_DD_=1000`） |
| **1トレードのリスク** | **$100** |
| **枚数** | **常に 2枚(固定)**(3口座とも共通) |
| **SL 上限** | **25pt**($100 ÷ $2.00 ÷ 2枚) |
| DAYGOAL | **$400** |

**★ SL 25pt を超えるシナリオでは、この口座だけが発注先から自動的に外れる。**
枚数・SL・TP は3口座とも同一で口座別に変えられないため(`order.py` の
`lines_for_account` は `account=` 行だけが口座依存)、LTA が使える 60pt 幅の
トレードでは LFF は不参加になる。全体は止まらない。ドライラン実測:

```
SL 24pt → リスク $96.00   発注先 LTA-A / LTA-B / LFF(3口座)
SL 30pt → リスク $120.00  除外 LFF00000000000002: リスク $120.00 が上限 $100.00 を超過
```

**★ R45(2026-08-27): この「1口座だけ外す」を発注経路の全段で通した。**
LFF 追加直後は `order.py` だけが口座別に除外し、その手前の2段が**経路全体**を
止めていた —— 実際に 02:49 の `BREAKER_CONTINUATION BUY A/ARMED`
(SL 27.75pt = $111)が、LTA 2口座では余裕で通るのに WATCH へ落ちている。

| 段 | 変更前 | 変更後 |
|---|---|---|
| `monitor_publish.normalize_qty` | 最も厳しい1本(=LFF 25pt)を超えたら **ARMED→WATCH** | 払える口座が1つでもあれば ARMED を維持し、外した口座を注記 |
| `execution_contract.evaluate` | 最も厳しい上限を超えたら `RISK_CAP_EXCEEDED` | `accountScope` を払える口座へ絞り、上限を残った口座の最厳値へ張り替える。**全口座が払えないときだけ** `RISK_CAP_EXCEEDED` |
| `order.py` | 既に口座別に除外(変更なし) | 変更なし |

外した口座は `executionContract.excludedAccounts` に残る。凍結上限は絞り込みの
**後**に適用するので、下流が上限を広げることはできない(狭い側だけが効く)。
**口座が1つも設定されていない経路**は「全口座が払えない」ではなく「判定材料が
無い」として従来どおり既定上限との単純比較に落ちる —— ここを取り違えると
口座設定を持たないテスト/縮退経路が丸ごと停止する。検証は
`tests/test_r45_account_exclusion.py`。

**★ R46(2026-08-27): 手動で進行中の建玉がある口座も、その口座だけ外す。**
LFF は手で回す口座なので、そこに建玉があるだけで LTA 2口座の自動発注まで
止まっていた（実測: 03:15/03:23 の両サイクルが
`[LFF...] autotrade hold: broker position ownership UNKNOWN (broker position
account is outside frozen scope)` で経路ごと hold）。判定は**所有権で自動**。

| 状態 | 扱い |
|---|---|
| その symbol/口座に凍結プランが1件も無い | **手動** → その口座だけ外す |
| 凍結プランの `accountScope` にこの口座が入っていない | **手動** → その口座だけ外す |
| identity 不明・照会 UNVERIFIED など「証明できないだけ」 | 従来どおり**経路全体を停止** |
| `NQX_AUTOTRADE_KILL=1` | 除外しない（KILLは台帳外の建玉も落とす明示経路） |

外した口座は `executionContract.excludedAccounts` に `reason: MANUAL_POSITION` で
残り、建玉には一切触れない。新規の宛先は `order.py --accounts` で凍結スコープへ
**狭める**（許可リストの部分集合のみ受理、`--flatten` / `--modify` には掛からないので
撤退経路は塞がらない）。plan の `accountScope` は「正本の凍結スコープ ∩ その周期の
許可リスト」で、増える方向には動かない。検証は
`tests/test_r46_manual_account_exclusion.py`。

**★ 残機 $1,000 ÷ 1R $100 = 10回。** §3 の警告どおり破産確率の観点では薄い
(定石は 20〜50 単位)。日次ガード(合算 −$1,160 / 2連敗 / 15分冷却)を確実に効かせる。

**★ DD 方式は EOD トレーリング**(2026-08-27 ユーザー確認)。LTA と同じなので
`TRAILING_DD_LFF00000000000002=1000` を設定し、残機は R44 の自動導出
（床 = max(EOD純資産の高値, 初回 netLiqSOD) − 1000）に任せる。手入力の
`LIFELINE_LFF00000000000002=1000` は残高を照会できないときのフォールバック。

**★ 口座スコープを変えると武装は自動解除される。** 実測で
`DISARMED (CROSSTRADE account scope changed since arming; re-arm in app)` になった。
Mini App から張り直すまで新規 ENTRY は出ない(設計どおりの挙動)。

**★ `maxAccounts` を変えたら Cloudflare Worker を再デプロイすること。**
`cloudflare/src/state_machine.js` は `execution_contract.json` を
**ビルド時 import** しており(1行目)、`normalizeAccountScope()`(507-514行)が
`accountMode.maxAccounts` を超えるスコープを `null` で弾く。ローカルの Python 側
だけ 3 にしても、デプロイ済み Worker が 2 のままだと **Mini App からの3口座武装が
サーバー側で拒否される**。

```bash
cd cloudflare && npm run deploy   # = wrangler deploy
```

### ★ 毎日やること: LIFELINE の更新

**DD が EOD で切り上がるので、`LIFELINE_*` は毎日その日の終値から手で更新する。**
ブローカーからは取得できず(`nqx_state.py`)、コードにトレーリングの実装も無い。
Phase 3 で枚数がこの値に比例するようになるため、**更新漏れがそのままサイズ誤りになる**。

EOD トレーリングは**大勝ちの翌日を危険にする**。床が上がるので、+$1,500 の翌日に
同額を吐き出すと開始時より不利になる。DAYGOAL($900)はそのための利確ブレーキで、
一貫性ルールのためではない(新条件に一貫性ルールは無い)。

### ★ 口座一覧は必ず実測で確かめる

```bash
python broker_status.py --accounts
```

**2026-08-24 の実測で `APEX0000000000010` が CrossTrade の一覧から消えていた。**
それが `CROSSTRADE_ACCOUNTS` に設定されたまま残っており、発注していれば宛先不明で
失敗していた。2026-08-21 にも同型の事故が起きかけている(§口座入替の履歴)。
**口座を触ったら必ずこの照会で裏を取る。**

`CROSSTRADE_ACCOUNTS` があると `CROSSTRADE_ACCOUNT` は**無視される**ので、
切り替えるときは必ず `CROSSTRADE_ACCOUNTS` を直すこと。

### 2口座ミラー経路（R40）

通常モードは `accountMode.kind=MIRRORED / maxAccounts=null`（= 口座数の上限なし。
2026-08-27 に 2 → 3 → 撤廃）。1つの正本シナリオと1つの ENTRY claim で
各口座の 2 MNQ（TP1 1枚 + runner 1枚）を送る。

**`null` と「キーが無い」は別物。** `maxAccounts` が `null` のときだけ無制限で、
キー自体が欠けている/契約が読めないときは従来どおり **1 に倒す**（狭い側が安全）。
Python(`execution_intent.normalize_account_scope` / `autotrade_arm._max_accounts`)と
Worker(`state_machine.normalizeAccountScope`)で同じ規則にしてある。

上限を消しても残る要件: **口座 ID は一意で空でないこと**、`^[A-Za-z0-9_-]{3,64}$` を
満たすこと、`RISK_*`/`DAYGOAL_*` と `CROSSTRADE_ACCOUNTS` の口座数が一致すること。
claim を口座ごとに複製しないため、同一シグナルの二重取得は起きない。

送信後は `broker_status.query_position(..., account=...)` と
`query_orders(..., account=...)` で各口座を独立照会する。position generation、
所有権、TP1後の `--modify --account`、`--flatten --account` も口座別であり、
Aの状態をBの管理根拠にしてはならない。4脚のどれかが不明なら再送せずHALTする。

### 口座入替の履歴(2026-08-21)

| 時点 | 発注先 | 残機 | 1R | SL上限 |
|---|---|---|---|---|
| 〜08-20 | Lucid 3口座 | 計 $1,359.5 | $105 | 52.5pt |
| 08-21 早朝 | `APEX0000000000009` | $3,000 | $180 | 45pt |
| 08-21 | `APEX0000000000010` | $4,000 | $240 | 60pt |
| **08-24 現在** | **LTA A/B 2口座ミラー** | **各$3,000**(EOD) | **各$240** | **各60pt** |

…0009 は1トレード(−$115)で入替。CrossTrade の `/accounts` からも消えており、
env の設定は `#(2026-08-21 封印/口座入替)` で保存してある。

**…0010 も 2026-08-24 の実測で一覧から消えていた。** 設定には残ったままだったので、
発注していれば宛先不明で失敗していた。`python broker_status.py --accounts` で
毎回裏を取ること。

### 旧 Lucid 3口座は経路から外した(2026-08-21)

CrossTrade の `GET /accounts` を実測した結果:

| 口座 | CrossTrade 側の状態 | 扱い |
|---|---|---|
| …0002 | **`unknown_account`**(存在しない) | 経路から削除 |
| …0004 | **`unknown_account`**(存在しない) | 経路から削除 |
| …0003 | 生存・FLAT(accountId 90000103) | **ユーザー指示で不使用**(残機 $370 に対し $180 は49%) |
| …0006 | — | Breached(終了) |

`.secrets/crosstrade.env` の `RISK_*` / `LIFELINE_*` / `DAYGOAL_*` は
**消さずにコメントで封印**してある(`#(2026-08-21 封印)`)。
Lucid に戻すときはコメントを外し `CROSSTRADE_ACCOUNTS` を書き戻すだけでよい。
バックアップ: `.secrets/crosstrade.env.bak-20260821`。

### 2枚固定が意味すること

MNQ 1枚 = 1pt $2.00 → **2枚 = 1pt $4.00**。**枚数で調整しない**と決めた以上、
**SL 幅がそのままリスク額**になる:

| SL 幅 | リスク(2枚) | 残機 $3,000 で何回負けられるか |
|---|---|---|
| 20pt | $80 | 37回 |
| 40pt | $160 | 18回 |
| **60pt(上限)** | **$240** | **12回** |

- **枚数を増やして「取り返す」経路は設計上存在しない**(§6 の破綻パターンで
  最も危険な操作を、口座設定の側で塞いだ形)。`order.py` の `MAX_QTY = 2` が
  天井としても効く
- **2枚固定は R8 の分割型を可能にする** — 1枚を TP1 で利確し、
  もう1枚をランナーとして走らせる(§6.4)。1枚固定では取れない構成

---

## 2. 商品仕様

| 項目 | 値 |
|---|---|
| シンボル | **MNQU6**(CME_MINI:MNQU2026) |
| **1pt** | **$2.00** |
| 1tick | 0.25pt = $0.50 |
| 手数料 | **往復 $1.90 / 枚**(2026-08-25 Tradovate 実測で較正。旧記載の $1.00 は誤り) |

**注意**: NQ(E-mini)は 1pt = $20.00。MNQ と混同しないこと。
過去に $0.50 と誤って計算し、金額を1/4に見積もった事例がある。

### ★ 発注水準から損益を計算しない(2026-08-25 に実測で判明)

**このプロジェクトは実約定価格を1件も取得できていない。** `broker_status.query_orders`
は生きている注文しか返さず、`.secrets/order_log.json` に残るのは**送信した価格**で
あって約定価格ではない。したがって発注水準(指値・ストップ)から出した損益は
**常に粗い上限**であり、ブローカーの実現損益とは一致しない。

**2026-08-25 の取引1**(Tradovate の約定明細で全項目が確定した唯一の実測):

| 脚 | 発注水準 | **実約定** | 滑り | 損益 |
|---|---|---|---|---|
| TP1(1枚) | 29,288.00 指値 | 29,288.00 | 0 | +$88.50 |
| RUNNER(1枚) | 29,221.00 ストップ | **29,219.75** | **1.25pt** | −$48.00 |
| 粗 | | | | **+$40.50** |
| 手数料 | | | | **−$3.80**(= $1.90/枚 往復) |
| 実現 | | | | **+$36.70** |

発注水準から出した見積もりは +$43.00 で、**$6.30 ずれた**。内訳は
**滑り $2.50 + 手数料の過小見積 $1.80**。

- **ストップ注文はトリガー価格では約定しない。** runner は 17:04:16 に
  29,219.75 で約定した(その足は始値 29,228.75 → 安値 29,214.25)。
- **端数で手数料の有無が分かる。** MNQ の 1tick = 1枚 $0.50 なので、価格だけで
  説明できる損益は必ず $0.50 の倍数。$36.70 の $0.20 は価格からは作れない。
- 2026-08-21 の −$117.00 → −$120.00($3.00 = $1.50/枚往復)は
  HANDOFF.md で「$5 の差は要突合」のまま放置されていたもの。実約定
  29,356.25 で計算し直すと差は $3.00 になる。手数料率が 8/25 の $1.90 と
  違うので、**滑りが混ざっている可能性が残る**(当時の明細は未取得)。

**運用ルール**:

1. 残高(`LIFELINE_*`)は**ブローカーの実現損益で更新する**。価格から計算した
   粗損益で更新しない。
2. リザルトは `result_from_report(..., legs=[...], reported_net=<実現>)` で作る。
   差は `fees` に入り、`dayguard` / `ledger.js` / `result.js` / `result_card.py`
   が全て同じ式で引く。
3. **訂正 publish は `result_id=` で元の ID を明示する。** `resultId` は素材の
   ハッシュなので、価格や時刻を直すと別 ID になり、DO の `resultLog` に
   **同じトレードの2行目**が積まれる。
4. 分割決済の `exit` は数量加重平均で、2脚だと 0.25 の刻みに乗らないことが
   ある(1/8 になる)。丸めた分は `fees` が吸収するので実現損益は合うが、
   **粗損益は脚から出す**(`result_card` はそうしている)。

---

## 3. リスク制限(厳守)

| 項目 | 上限(LTA / 残機 $3,000) |
|---|---|
| **1トレードのリスク** | **$240**(2026-08-24 更新 = 契約上限。Phase 3 で **$300 = 残機の1/10** へ) |
| **枚数** | **常に 2枚。固定。増やさない**(Phase 3 でリスク額からの逆算に変わる) |
| **SL 幅** | **最大 60pt**($240 ÷ $2.00 ÷ 2枚) |
| **1日の許容損失** | **各口座$480 / 2口座合算$960**(= 1シグナル合算$480 × 2連敗)。これが真の制約 |
| **1日の利益上限** | **DAYGOAL $900**(下記)。到達したら即やめる |
| **連敗** | **2連敗でその日終了** |
| **損切り後** | **15分は新規なし**(冷却) |
| **1日の発注回数** | **上限なし**(2026-08-18 撤廃。下記の理由) |
| SL | **必須**。未指定の発注はしない |

**★ 枚数は交渉の対象ではない。** 「2枚固定」はリスク調整の手段を
**SL 幅だけ**に限定するという設計判断。構造が 60pt を超える SL を要求するなら、
**枚数を1枚に減らして通す運用はしない** — そのトレードは見送る
(§6 初期ストップの手順5)。ここを緩めた瞬間に「枚数で調整する」思考が戻る。

### 回数制限を撤廃した理由(2026-08-18)

旧ルールの「3回/日」は **3回 × $60 = $180 = 当時の日次損失上限**と同じ数字で、
**同じ制約を二重に書いていた**。下方向には冗長で、1・2戦目が勝った日でも
3回で止まるため**上方向には純粋な機会損失**だった。

8/7 を実際に止められたのは回数ではなく**損切り直後の再エントリー禁止**
(08:03〜08:09 の6分5往復)。よって回数ではなく spiral の入口を直接塞ぐ:
**各口座日次損失−$480・2口座合算−$960 / 2連敗で終了 / 損切り後15分の冷却**。この3つが揃えば
回数で縛る必要がない。

### 日次利益上限(DAYGOAL)

**新条件に一貫性ルールは無い。** 旧 Apex 時代の算式
`DAYGOAL = min(利益目標 × consistency上限, 残機)` は**根拠が消えた**。

代わりに DAYGOAL は **EOD トレーリング DD に対する利確ブレーキ**として意味を持つ。
床が終値で切り上がるので、大きく勝った翌日に同額を吐き出すと開始時より不利になる。
1日の利益をある程度で止めれば、床の上がり方と戻しの幅が釣り合う。

| 口座 | 残機 | 目標 | **DAYGOAL** | 根拠 |
|---|---|---|---|---|
| LTATANOBA1000000000001 | $3,000 | $6,000 | **$900** | 日次損失上限 −$480 のおよそ2倍。勝ち日を伸ばしすぎない |

`.secrets/crosstrade.env`:

```
DAYGOAL_LTATANOBA1000000000001=900
```

**`LIFELINE_*` を更新したら `DAYGOAL_*` も見直す。**

### LTA の算術(2026-08-24)

| 項目 | 値 | 意味 |
|---|---|---|
| 必要利益 | $6,000 | |
| 残機 | $3,000(EOD トレーリング) | |
| 比率 | **2.0 : 1** | 残機の2倍を稼ぐ必要がある |
| 1R(Phase 2) | $240 | 残機の 8%。12.5回分 |
| 1R(Phase 3 予定) | $300 | 残機の 1/10。**10回分** |
| 必要 R 数 | $240 なら 25R / $300 なら **20R** | 勝率と RR 次第 |

**期限が無い(最低5日・上限なし)ことが最大の武器。** 時間を味方にできるので、
1トレードを大きくして早く通す理由が無い。ULTRA(目標から逆算した大枚数で
1〜3回勝負)の存在意義は「速く通す」ことなので、この条件では合理性が薄い。

**⚠ 勝率・期待値は依然として未知。** 実トレード記録は1件しかなく、403本の
サイクル記録に約定結果が1件も含まれていない。「20R 必要」は算術だが、
「何トレードで届くか」は**予測できない**。最低5日を較正期間として使い、
実測が貯まってからサイズを見直すのが筋。

**残機10回分は一般的な目安より薄い。** 破産確率の観点では 20〜50 単位が定石で、
10 単位は 10 連敗で即死する。期限が無い以上、単位を薄くする理由は時間短縮以外に無い
—— それでも 10 で行くという判断であれば、日次ガード(2口座合算−$960 / 2連敗 / 15分冷却)を
確実に効かせること。

$240 は MNQ で **2枚 60pt**。`order.py` がこの上限を強制する(超過は送信前に拒否)。

### 多口座に同時発注する場合(口座別のリスク上限)

**共通の1つの上限では小さい口座を守れない。** 口座ごとに残機が違うので、
`.secrets/crosstrade.env` に口座別で書く（現行は LTA A/B のミラー経路）:

```
CROSSTRADE_ACCOUNTS=LTATANOBA1000000000001,LTATANOBA1000000000002
RISK_LTATANOBA1000000000001=240
LIFELINE_LTATANOBA1000000000001=3000
TARGET_LTATANOBA1000000000001=6000
DAYGOAL_LTATANOBA1000000000001=900
TRAILING_DD_LTATANOBA1000000000001=3000
RISK_LTATANOBA1000000000002=240
LIFELINE_LTATANOBA1000000000002=3000
TARGET_LTATANOBA1000000000002=6000
DAYGOAL_LTATANOBA1000000000002=900
TRAILING_DD_LTATANOBA1000000000002=3000
NQX_DAY_LOSS_LIMIT=-960
```

`NQX_DAY_LOSS_LIMIT` は**全口座の合算に対する1つの閾値**。同じシグナルの
最大損失は $240 + $240 = **$480** なので、2連敗停止と整合する合算値は **-$960**
（3口座の時代は LucidPro $200 を足して $680 × 2連敗 = -$1,360 だった）。

`TRAILING_DD_*` が入っている口座は、残機がブローカーの netLiq から自動導出される
（R44）。手入力の `LIFELINE_*` は残高を照会できないときのフォールバック。

**★ 口座を増減したら4点を必ず同時に動かす。** `CROSSTRADE_ACCOUNTS` /
`RISK_*`・`DAYGOAL_*` / `NQX_DAY_LOSS_LIMIT` / **`cloudflare/wrangler.toml` の
`NQX_AUTOTRADE_ACCOUNTS`(直したら `npm run deploy` まで)**。4点目を忘れると
AUTO が武装できなくなる(2026-09-01 に実際に取りこぼした)。
`dayguard.configured_accounts()` は
`CROSSTRADE_ACCOUNTS` ではなく `RISK_*`/`DAYGOAL_*` のキー名から口座を逆引きする
(`dayguard.py:201-210`)ので、片方だけ残すと日次損益が違う倍率で集計される。

`RISK_<口座ID>` が無い口座は `MAX_RISK_DOLLARS` にフォールバックする。

**上限を超える口座はその口座だけ発注先から外れる**(全体は止めない)。
MNQ は1枚が最小なので枚数では調整できず、超えたら見送るしかない。
全口座が上限を超えた場合のみエラーで停止する。

```
リスク: $240.00  リワード: $480.00  R:R = 1:2.00
  発注先 APEX0000000000010: 上限 $240.00
```

- **成行は `--last`(発注直前に観測した現在値)が必須**(2026-08-16 再設計。
  口座数に関係なく必須)。リスクは
  **(|現在値 − SL| + 滑り緩衝 `MARKET_SLIPPAGE_PT`・既定 2pt) × 枚数 × $2.00**
  で概算し、指値と同じ口座別上限で発注先を振り分ける。SL/TP の向きも
  現在値を基準に検証される。概算なので、実約定が緩衝を超えて滑る
  可能性はゼロではない
- **`--flatten` は除外の対象外。** 全口座に必ず飛ぶ(決済を取りこぼさない)

### SL幅が小さい口座の寿命を決める

1枚が最小なので、SL幅がそのまま「あと何回負けられるか」になる。

| SL幅 | リスク(2枚) | 残機 $3,000 での残り回数 |
|---|---|---|
| 15pt | $60 | 50回 |
| 30pt | $120 | 25回 |
| 45pt | $180 | 16回 |
| **60pt(上限)** | **$240** | **12回** |

**枚数は常に2枚。** SL 幅がそのまま残機の消費速度になる。

---

## 4. 発注方法

### スクリプト

```bash
cd "C:/Users/exexu/Downloads/nq-nightwatch-claude-code-handoff"

# 分割型ドライラン(送信されない)。TP1とrunner最終TPは異なる価格。
python order.py --side sell --qty 2 --entry 29585 --sl 29610 --split-tp 29548,29510

# 分割型の実際送信。各1枚の独立OCOブラケットとして送られる。
python order.py --side sell --qty 2 --entry 29585 --sl 29610 --split-tp 29548,29510 --confirm

# 成行分割(--last = 発注直前に観測した現在値。これを基準にリスクを概算する)
python order.py --market --side buy --qty 2 --sl 29450 --split-tp 29520,29580 --last 29473 --confirm

# 全決済
python order.py --flatten --confirm

# 本日の発注状況
python order.py --status
```

### 経路

```
order.py → CrossTrade Webhook → Tradovate(デモ口座)
```

- 認証情報は `.secrets/crosstrade.env`(**共有・コミット禁止**)
- 発注履歴は `.secrets/order_log.json`(監査・dayguard の実績入力に使用)
- **`--split-tp TP1,TP2` は同一SLを持つ各1枚の独立OCOブラケットになる**。
  自律エンジンはこの分割型だけを使い、単一`--tp`やTPなしは手動ドライラン等の
  後方互換経路であって自律発注計画には使わない
- ブラケットは **Tradovate サーバー側で管理**。PCを落としても機能する
- 指値が約定しなければブラケットも発動しない(orphan 防止)

### 安全装置(order.py に実装済み)

- `--confirm` なしでは**絶対に送信されない**
- 3枚以上は拒否
- SL 未指定は拒否
- SL/TP の向きが逆なら拒否
- 固定の1日回数上限は撤廃済み。日次損失・連敗・冷却を優先し、必要時だけ
  `NQX_MAX_ORDERS_PER_DAY` を正数へ設定する
- R:R を自動計算して表示

### 自律送信の許可境界(2026-08-24 Mini App AUTOへ統一)

認証済みTelegram Mini AppでユーザーがAUTOをONにした期限内だけ、
`autotrade_engine.py` は `--confirm` 付きの新規ENTRYを送信できる。AUTO権限は
Worker/Durable Objectが発行する7時間のサーバー状態で、localStorageや一時的なシェル
環境変数を正本にしない。AUTO OFF・期限切れ・サーバー状態未確認・口座/限月不一致は
新規を即時停止する。実送信は broker建玉/注文照会、正本publish、R11-D A+/A/B(2026-09-04 に B 追加)、イベント、
日次損失、リスク、SL/TP向き、二重送信台帳を全て通す。AUTO OFF後も凍結済み所有建玉の
OCO/runner管理と必要な決済は放棄しない。

---

## 5. 提示フォーマット(厳守)

SL/TP/エントリーは**必ず価格そのもの**で提示する。
損益額や pt 幅、曖昧な表現(`29,580 下`、`29,630 圏`)だけで提示しない。

```
Entry: 29,630.00
SL:    29,605.00
TP:    29,680.00

リスク $100.00(25pt × 2枚) / R:R 1:2.0
根拠: VAH 29,631.5 の直下、CVO シグナル下抜け
```

(25pt × 2枚 = $100 は LTA の上限 $240 に収まる。
§1 のとおり **枚数は常に2枚**・**SL は 60pt まで**が発注可能域)

pt幅と金額は**補足**として併記する。

---

## 6. ユーザーの破綻パターン(最重要)

ユーザーは過去に複数回、口座を破綻させている。原因は自分で
**「取り逃しへの反応」**と特定済み。

### 原則1: トレール幅は「刈られない距離」ではなく「守りたい額」で決める

刈られない距離は存在しない。距離を決めた時点で、その幅の押しで
刈られることは**設計上受け入れている**。

刈られた後に「もっと緩くすべきだった」とは**言わない** —
結果論であり、次回の過剰な緩和を招く。

#### ⚠ 原則1は「トレール専用」。初期ストップに使ってはいけない

原則1 は**含み益が出た後**にどれだけ利益を守るかの話。
これを初期 SL に適用すると、**金額を小さくすることが目的化して
構造より内側にストップを置く**ことになる。

実際に 2026-08-15 に発生した。1枚 $60 枠(= 30pt 使える)に対して
**SL 13pt** で提示した。当日の3分足の値幅は 13.5〜16.75pt、平均15pt。
**「普通の1本」で刈られる幅**を、金額が小さいという理由で採用していた。

```
その日の推移: SL 26pt($52) → 21pt($42) → 13pt($26)
金額が小さくなるほど「安全」に見えて、刈られる確率だけ上がっていた
```

### 初期ストップの決め方(この順序を飛ばさない)

```
1. トレードが否定される価格はどこか(構造)
2. + 直近バーの値幅ぶんの緩衝(ヒゲに刈られない距離)
3. = SL距離
4. 固定2枚でリスク = SL距離 × $2.00 × 2 を計算する
5. $240 / 60pt に収まらない → そのトレードは取らない（枚数を減らさない）
```

**R90(2026-09-15)の追加規則(docs/R90_STOP_LOGIC_HOLES.md)**: (a) 手順 2 の緩衝を置いた
SL が **VWAP の近く(±1N)** にあれば、SL を VWAP の外側 0.25N まで逃がし、手順 5 の予算判定は
その SL で行う(VWAP の手前に置いた SL は VWAP を試す動きで刈られる。09-14 23:26 の実例)。
(b) 成行に切り替わるときは **発注時点の価格から SL まで 1N 以上**なければ出さない。予定建値で
通ったノイズ床の距離は、成行約定の瞬間に消えている。

**リスク枠は「上限テスト」ではなく「予算」。** 収まったから終わりではなく、
構造が要求する距離を先に決めて、そこから枚数を出す。

枠 $240・2枚なら **60pt まで使える**。構造が20pt要求するなら20pt取る。
$240 に収まらないなら、**そのトレードを見送る**
(枚数は2枚固定と決めたので、収まらない時点で場所が悪い)。

**「まともな SL」と「近いエントリー」が両立しない場所は、
そもそもエントリー適地ではない。**

### 原則2: 刈られた直後の入り直しをしない

ストップ執行後に価格が伸びた場面では、取り逃し額を認めた上で
**確定額は奪われないこと**を示し、明示的に「今から入り直さない」と伝える。

### 原則3: 短時間での往復売買・サイズ倍増を検知したら即座に止める

- **数分で複数回転** = コントロール喪失の最も早い兆候
- **サイズが前回の2倍を超えた時点で警告**する

### MSNR フルドクトリンと口座枠の相性(2026-08-21 改訂)

旧注記($60 枠では TP1 1.8R = 45pt が薄い時間帯にしか成立しない)は失効した。
現行: **SL 上限 60pt / TP1 1.8R ≤ 108pt** は NY の通常ボラで十分成立する。
TP2(2.8R)/ TP3(4.0R)は CLAUDE.md §2 の管理計画で表現する。

**2枚固定の自律経路は分割型のみ。** 1枚目をTP1(1.8R目安)、2枚目をrunner最終TPへ
各1枚の独立OCOブラケットとして出す。TP1約定後に正本照会でqty=1を確認してから、
runnerだけを建値→トレールへ変更する。**SL ≤ 60ptかつTP1/runnerが異なる価格**が条件。

この管理計画5項目は **CLAUDE.md §4** に従い、エントリー提示と同時に凍結する。

口座算術の天井:
- DAYGOAL **$2,700**(11.25R)に**到達で新規が止まるだけで、保有中の
  ランナーは強制決済されない**(dayguard は発注前判定のみ。R7 実測)
- 必要利益 $9,000 ÷ 1R $240 = **37.5R**。RR 1:2 で勝率5割なら75トレード、
  **RR 1:4 なら約半分**。この口座では High RR 化が直接効く

### 等級(grade)とボラ帯の関係(2026-08-18・R4)

`msnr_gate` は通った提案に **A+ / A** を付ける。A+ は
「初動 ≥1.3×ノイズ床」「合流あり or 上位階層(tier ≤2)」「鮮度が
FRESH / WICK_TESTED」の3条件すべて。

- 比率 ≤0.6 — grade は問わない。**A でも武装する**

**★ 2026-09-04 ユーザー決定: B も発注可能にした。** `execution_contract.json` の
`allowedGrades` を `A / A+ / B` にし、msnr_gate・pipeline・publish・Bot・Worker・
Mini App の等級ゲートを同じ集合へ揃えた。等級は順位付け(A+ > A > B)にだけ残る。
B のエッジは未実測なので、R48 スコアカードの `grade` 別集計で PF を追うこと。
- 比率 >0.6 — grade に関係なく全停止(不変)

**★ 2026-09-01 ユーザー決定: 「比率 0.4〜0.6 は A+ のみ」を撤廃した。**
`monitor_publish.VOL_GATE_APLUS` の既定を stand-down と同値(0.60)にして
A+ 限定帯を空にしてある。実際に 23:22 / 23:26 の
`BREAKER_CONTINUATION BUY A/ARMED` が比率 0.54〜0.60 で WATCH へ落ちていた。
**戻すのはコード変更ではなく環境変数** `NQX_VOL_GATE_APLUS=0.40`。

この帯で取ると何が起きるかは正確に把握しておく: 比率 0.54 は
**ノイズ床 32pt に対し SL 予算が 60pt** ということで、構造SLの緩衝
(最低 `1.0 × noiseFloor`)だけで予算の半分以上が消える。SL は $240 の
上限に収まっても「普通の1本」に近い幅で刈られやすくなる —— §6 の
初期ストップの手順は緩めない(枚数は2枚固定、SLを内側へ縮めない)。

---

## 7. 実績と失格記録

| 日 | 損益 | 内容 |
|---|---|---|
| 8/3 | +$1,500 | 指標ミス −$177.5 から立て直し。28,540 押し目支持帯 |
| 8/4 | +$618 | **VAH 29,630 スナイプ**。TP1 → 建値ストップ → トレール |
| 8/5 | −$759.50 | 支持帯が次々貫通する日。2連敗後も続けて追加 −$312.50 |
| 8/6 | +$1,500 | VWAP 下抜けを待ってピラミッディング。建値ストップでリスク$7.40 |
| **8/7** | **−$2,816** | **失格** |
| 8/10 | +$242 | 6約定・2枚 |
| 8/12 | +$316 | 2戦2勝・4約定・2枚 |
| 8/17 | −$112.50 | **3口座運用の初日。**…0002 −$38.5 / …0003 −$37.0 / …0004 −$37.0。1約定・1枚 |
| **9/1** | **LTA +$944.00 / LucidPro −$2,049.50** | **LucidPro 50K が残機超過で失格**(床 $48,000 を $49.50 割り。1R $200 の 10.2 回分を1日で消費)。LTA-A +$452.40 / LTA-B +$491.60 は無傷。詳細は §1「2026-09-01」 |

### 8/17(3口座運用の初日)

規律面は守れた。**1約定・1枚・−$38.5(日次撤退ライン −$180 の21%)**で、
8/7 型の往復売買やサイズ倍増は一切なかった。損切り後の入り直しもしていない。

失敗は**型の選択**にあった。回転相場(確定足シグナルが次の足で否定される
状態)が7時間続いていたのに、その環境に最も弱いブレイク→リテスト型を
2度武装させた。詳細と再発防止は CLAUDE.md §9「8/17 の教訓」。

### 8/7 の崩壊過程(数字)

**目標 $3,000 まで残り $141.50 の地点から、2時間で失格。**

- 07:02 開始。必要額は **$141.50 のみ**(1枚 × 71pt で達成)
- **08:19 時点の累計 −$199** ← ここまでは規律内
- そこからサイズが **2枚 → 5枚 → 10枚 → 20枚 → 21枚 → 24枚**
- **2時間で約定66回、総ロット520**、手数料だけで **$260**
- 08:03〜08:09 の**6分間で5往復**(全て5枚・全て小損失)
- 単発最大損失: 21枚で **−$619.50**、24枚で **−$618**

**相場のせいではない**: 当日のレンジは 29,510〜29,597 の約87pt、ごく普通の膠着。
この相場で $2,816 失うには、サイズを10倍にして66回売買する必要があった。

**教訓**: 目標達成が目前(残額が1トレードで届く)のときこそ、
**「取ったら即やめる」を最優先で伝える**。残りが小さいほど危険度が上がる。

---

## 8. ユーザーの強み(尊重すべき点)

**構造的根拠を自分で見つける力がある。**

8/4 のエントリーは、エージェントが提示した根拠の薄い 29,610 ではなく、
ユーザー自身が指摘した **VAH 29,631.5** を採用した結果、
そこから一度も割らずに +268pt 伸びた。

**レベルの根拠を問う姿勢を尊重すること。**
エージェント側の提示が構造的に弱い場合、ユーザーの指摘の方が正しいことが多い。

---

## 9. 分析上の注意点(実際に間違えた事例)

- **形成中バーで方向を断定しない**(8/5に誤判定)
- **VWAPバンドの極端な収縮は、セッションリセット直後のデータ不足の可能性**
  (セッション開始から30分〜1時間はバンドを判断材料にしない)
- **VIX の分足に異常値(スパイク)が混入することがある**。前後のバーと
  比較して1本だけ飛んでいれば、データのグリッチとして扱う
- **VIX はガンマの代理にしない**(NQX不変条件11)
- **CVO は累積型**。上昇継続中は当然増え続けるので、天井は教えてくれない
- 約定履歴を評価するときは、**ストップの位置と設計意図を確認してから**判断する
  (8/6に「10枚は危険」と誤評価。実際は建値ストップでリスク$7.40だった)

---

## 10. 監視スケジュール(NYセッション)

| フェーズ | JST | ET | 間隔 |
|---|---|---|---|
| 監視 | **07:00 – 翌05:45** | 18:00 – 16:45 | **3分** |

2026-08-17 ユーザー指定で一本化(旧: 22:00-00:45 15分 / 01:00-04:00 30分)。
2026-08-25 ユーザー指定で開始を 21:00 → 12:30 JST へ前倒し(欧州・アジア後場も監視対象)。
**2026-09-01 ユーザー指定でさらに 12:30 → 07:00 JST へ前倒し。** 平日の休止帯は
**04:00〜07:00 の3時間だけ**になった。月曜 07:00 JST = 日曜 18:00 ET = CME 週初の寄り。
窓は 21 時間で、AUTO の連続武装上限(`MAX_MINUTES=720` = 12時間)より長い。
**1回の武装では窓全体を覆えない**ので、後半も自律送信を使うなら人が張り直す。
窓の外ではループを回さない。トークン節約はサイクル内で行う
(報告1-2行・study_values 数値読み・スクリーンショット不使用)。

---

## 11. 環境上の注意

- **TradingView MCP は CDP で TradingView Desktop に接続している**
- **Claude Code と Codex を同時起動すると競合する可能性がある**。
  どちらか一方を使うこと
- チャートの時間足や銘柄がユーザー操作で変わることがある。
  分析前に `chart_get_state` で確認し、必要なら `chart_set_timeframe` で戻す
- VIX は pane 1(`CBOE_DLY:VIX`)。`pane_focus` で切り替えて取得し、
  取得後は `pane_focus index=0` で NQ に戻す
